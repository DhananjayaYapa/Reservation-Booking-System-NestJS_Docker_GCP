# Disable pnpm's supply-chain release-age check.
# It fetches registry metadata for every lockfile entry, which times out
# inside Docker builds (ERR_PNPM_META_FETCH_FAIL). 0 = no minimum age.
minimum-release-age=0

# Trust the local store/lockfile before hitting the network.
prefer-offline=true
